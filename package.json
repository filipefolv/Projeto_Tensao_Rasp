{
  "name": "tensao-rasp",
  "version": "3.0.0",
  "scripts": {
    "start": "vercel dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.7.0",
    "@vercel/postgres": "^0.4.1",
    "cors": "^2.8.5",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@vercel/node": "^2.15.4"
  }
}